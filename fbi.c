// hack the fbi
#include <stdio.h>

int main() {
    int percents;
    printf("Hakcing FBI: %d", percents);

    for (int percents = 1; percents <= 100; percents++) {
        printf("Hakcing FBI: %d\n", percents);
    }
}
